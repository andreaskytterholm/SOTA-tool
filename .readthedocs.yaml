version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.13"
  jobs:
    post_install:
      - curl -LsSf https://astral.sh/uv/install.sh | sh
      - uv sync

mkdocs:
  configuration: mkdocs.yml
